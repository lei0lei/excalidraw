/**
 * Insertable templates for excalidraw-app. Organize by domain folder (`uml/`, …).
 *
 * - Import from `excalidraw-app/templates` for app-wide use.
 * - Import from `excalidraw-app/templates/<domain>` when a feature only touches one domain.
 */
export * from "./shared";
export * from "./uml";
